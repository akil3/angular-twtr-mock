import { ParsetweetPipe } from './parsetweet.pipe';

describe('ParsetweetPipe', () => {
  it('create an instance', () => {
    const pipe = new ParsetweetPipe();
    expect(pipe).toBeTruthy();
  });
});
